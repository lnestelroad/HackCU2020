#!/usr/bin/env python3

from distutils.core import setup

setup(name="Foodbank",
        version=1.0,
        description="Food bank tracker in flask",
        package_dir={'': 'src'},
        packages=["foodbank"])
